# Release Notes

## [1.0.1]

长期连接稳定性修复

- **修复渲染缓存不同步**：输出历史达到上限（1 万条）后 `render_rows` 未同步弹出，导致新数据不显示（RX 计数持续增长但输出区卡住），需 `:clear` 才能恢复；现同步维护，长时间运行输出持续正常
- **修复无换行数据流不显示**：`LineAssembler` 超时判定由"距上次输入"改为"距上次输出"，持续无换行的数据流也能周期性刷出；新增 16KB 强制刷出保护，防止内存无限增长
- **RX 数据背压降载**：读线程采用有界通道 + `send_timeout` 背压，设备高速输出时主循环不被数据占满，命令输入保持灵敏；极端超载时丢弃并提示，杜绝内存无限增长
- **命令输入响应优化**：主循环每帧限制 RX 数据消费量，保证输入事件有固定响应窗口
- **日志写入缓冲**：日志文件改用缓冲写入，消除高频磁盘 IO 阻塞主循环

---

## [1.0.0]

串口终端 — 专为 Windows Terminal 集成，基于 Rust 完整重构

- **串口连接配置**：支持端口、波特率、数据位、停止位、校验位、流控的完整配置，提供图形化设置面板与命令行双入口
- **中文编码支持**：内置 `encoding_rs` 流式解码，支持 UTF-8 / GBK / 自动检测 / 十六进制（Hex）视图，跨数据块拆分的中文字符也不会乱码
- **实时收发**：独立的读/写线程，读写句柄分离，长写超时避免设备繁忙时写入失败，界面永不卡顿
- **Windows Terminal 原生体验**：作为 Windows Terminal Profile 启动，与终端标签页、快捷键、鼠标操作无缝融合
- **现代 TUI 界面**：三区布局（状态栏 / 输出区 / 输入区），内置 8 套配色主题（GitHub Dark、Dracula、Monokai、Solarized Dark、Nord、One Dark、Gruvbox、Tokyo Night），长行自动折行，鼠标滚轮浏览历史
- **设备信息识别**：自动识别常见 USB 串口芯片（CH340、CP210x、FTDI、PL2303、ESP32 全系、STM32、RP2040 等 100+ 款），在设置面板显示芯片型号、厂商与 VID/PID
- **命令系统**：纯命令驱动，支持动态补全（命令名、参数、串口名、日志文件四级补全）
- **日志能力**：`:log` 实时记录、`:export` 导出日志、`:replay` 按原始时序回放（支持倍速）
- **搜索定位**：`:search` / `:find` 在历史日志中搜索关键词并高亮定位
- **最近设备记忆**：自动记录最近使用的设备参数，`:recent` 一键重连
- **断线自动重连**：设备拔出/重插自动恢复连接
- **单文件便携**：单个 `serial-term.exe`，免安装、免注册表

---
